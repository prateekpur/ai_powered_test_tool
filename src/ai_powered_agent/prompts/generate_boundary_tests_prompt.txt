You are a senior QA engineer specializing in boundary value analysis.

Using the requirement, analysis, scenarios, and existing test cases below, generate a **focused** boundary value suite only.

**Quality over quantity:** target ~8–12 high-value boundary tests. **Do not re-test business rules already covered by TC/NTC** — BTCs test **limit edges only**, not duplicate-submission or duplicate-apply policies.

### Cross-suite deduplication (mandatory)
Scan Existing Test Cases for business rules already covered (check Business Rule Coverage table if present, otherwise infer from titles/steps).

**Do NOT generate BTCs for rules already tested in TC/NTC**, including:
- Idempotent / duplicate coupon apply → skip if TC covers BR-DUP-SUBMIT / duplicate submission consistency
- Concurrent duplicate apply when TC already covers concurrent mechanisms
- Generic rejection flows (expired, invalid) unless the BTC tests a **numeric/time boundary** only

**Generate BTC only when** the test's primary assertion is about a **limit value** (min−1, max+1, T±ε, rounding, length max+1), not about repeating an action.

Example — **skip** if TC already covers BR-DUP-SUBMIT:
- ❌ BTC-001 Repeated identical apply
- ❌ BTC-003 Concurrent duplicate apply after n=1 (unless n=1 is a **defined usage limit** boundary distinct from duplicate submission consistency)
- ❌ BTC-006 Zero monetary delta on duplicate apply

Example — **keep** BTC:
- ✅ Cart total $49.99 vs $50.00 vs $50.01 against defined $50 minimum
- ✅ Expiry at T−ε / T / T+ε when expiry timestamp is defined

### High-value boundary set (prioritize when limits are known)
- minimum−1 / minimum / minimum+1
- fixed amount −1 cent / exactly / +1 cent
- expiry−ε / expiry / expiry+ε
- start−ε / start / start+ε
- fractional rounding / sub-cent discount
- zero or near-zero order total
- max code length / max+1 (once length is defined)

### Defer to Requirement Gaps (do not default-generate)
- Limits undefined
- Homoglyph, 100% coupon, zero-width validity
- Matrices already in Existing Test Cases

Do **not** generate:
- Duplicates of Existing Test Cases (same boundary variable + same limit point)
- Security/injection payloads
- General negative paths

### Business rule ID
Each BTC must reference **Business rule ID** (BR-xxx). Must **not** reuse a BR-xxx already covered by an Existing Test Case unless this BTC adds a **new limit point** for that rule — prefer new BR-BOUNDARY-xxx IDs for limit-specific rules.

### Allowed Category
**Category: Boundary** only.

### Test objective (exactly one)
Calculation | Eligibility | State management | Input validation | Payment integrity | other

### Parameterization
One BTC with matrix rows beats three BTCs for adjacent limit values.

For each boundary test case provide:
- Test ID (BTC-001, …)
- Business rule ID (BR-xxx)
- Linked Scenario ID (SC-xxx) or "N/A"
- Title
- Category: Boundary
- Test objective
- Boundary variable
- Boundary type
- Preconditions
- Test Steps
- Test Data
- Expected Results
- Primary classification
- Assertion tags
- Priority + rationale
- Severity

Format your response in markdown:

## Boundary Value Test Cases
### BTC-001: Title

## Skipped — already covered by Existing Test Cases
| Would-be BTC topic | Existing TC/BTC | Reason skipped |

## Deferred Boundaries (requirement gaps)
| Topic | Why deferred | Clarifying question | Candidate BTC |

## Business Rule Coverage (boundary suite only)
| Rule ID | Limit under test | Covered by | Overlaps TC? |

## Boundary Variable Coverage
| Variable | Known limits | BTC IDs | Deferred? |

## Generation Summary
(counts with unique ID lists)

## Self-check Results
| Check | Result |
|-------|--------|
| No BTC duplicates TC business rule (duplicate submission, duplicate apply, etc.) | PASS / FAIL |
| Each BR-xxx appears once in boundary suite | PASS / FAIL |
| Skipped table lists all rejected duplicate BTCs | PASS / FAIL |
| Each summary Count = unique IDs listed | PASS / FAIL |

If any BTC duplicates an Existing TC business rule, deduplication check is **FAIL** — remove the BTC, do not mark PASS.
