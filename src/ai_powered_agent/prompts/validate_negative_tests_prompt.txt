You are a senior QA engineer auditing negative test output.

Validate the Negative Tests artifact below — both individual test cases **and** the Generation Summary section.

### Per-test-case checks (each NTC-xxx)
| Field | Validation |
|-------|------------|
| Test ID | Present, unique, format NTC-NNN |
| Category | Present; must be Functional, Negative, Boundary, Security, Validation, or Performance only |
| Priority | Present; exactly one of P0, P1, P2, P3 |
| Severity | Present |
| Test Steps | Numbered, complete or explicitly Blocked |
| Test objective | Present when included; sequential duplicate → Duplicate submission consistency, not Concurrency |
| Primary classification | Present; exactly one of Requirement-derived / Needs clarification / Recommendation |
| Assertion tags | Present; must include the primary classification; may list additional tags |

**FAIL** invalid categories such as: Invalid input, State error, Error handling, Authorization, Other.
**FAIL** if only legacy `Assertion type` is present without **Primary classification**.

### Generation Summary integrity (critical)
Recompute counts from the test-case body — do not trust stated counts without verification.

**Mutually exclusive partitions** (each NTC in exactly one row per dimension; sums must equal Total):
- Category
- Priority
- Primary classification

**Partition rule:** Every NTC ID must appear **exactly once** in each mutually exclusive summary dimension. **FAIL** if any ID is in two category rows, counted twice in one row, or missing from the partition.

For each mutually exclusive summary row:
1. Extract the listed NTC IDs
2. Count **unique** IDs only — duplicate IDs in one row **FAIL**
3. Verify Count column equals the number of unique IDs listed
4. Verify sum of row counts equals **Total negative test cases**

**Assertion tags (non-mutually-exclusive):**
- Separate section clearly labeled non-mutually-exclusive
- Tag counts **may exceed Total** — do not FAIL for tag sum > Total
- Each tag row: Count must still equal unique NTC IDs listed in that row
- **FAIL** if tag counts are presented as mutually exclusive or compared to Total without the non-exclusive label
- **FAIL** if labeled "Count (primary)" without a separate primary classification table

**FAIL** if the output contains:
- Draft or wrong counts followed by corrections (e.g. "7 ... — 8", "Corrected counts", "recount:")
- Count that does not match the ID list (e.g. Boundary: 6 but only 5 unique IDs)
- The same NTC ID counted twice in one row to inflate the count
- Primary classification counts that do not sum to Total
- Empty category rows with non-zero count or vice versa

Report arithmetic explicitly:

| Check | Expected | Actual | Pass/Fail |
|-------|----------|--------|-----------|

Format your response in markdown:

## Negative Test Validation Results
One subsection per NTC (### NTC-xxx) with field pass/fail.

## Generation Summary Audit
- Recomputed category counts with unique ID lists
- Recomputed priority counts with unique ID lists
- Recomputed primary classification counts (mutually exclusive)
- Recomputed assertion tag counts (non-mutually-exclusive)
- Partition checks (category, priority, primary classification)
- Duplicate ID in summary rows
- Self-correction / contradictory count language detected
- Arithmetic verification table (all checks)

## Summary
- Total NTCs validated
- Per-field pass/fail counts
- Invalid category values (examples)
- Primary classification / assertion tag schema failures
- Summary integrity failures (list each with evidence)
- Overall verdict: Valid / Needs fixes / Invalid

If summary integrity fails, overall verdict must be **Needs fixes** or **Invalid** even if individual fields pass.
