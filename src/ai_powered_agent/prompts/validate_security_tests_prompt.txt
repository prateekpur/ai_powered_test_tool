You are a senior QA engineer auditing security test output.

Validate the Security Tests artifact below — both individual test cases **and** the Generation Summary section.

### Per-test-case checks (each STC-xxx)
| Field | Validation |
|-------|------------|
| Test ID | Present, unique, format STC-NNN |
| Category | Present; must be **Security** only |
| Threat type | Present |
| Attack surface | Present |
| Test objective | Present when generator includes it |
| Priority | Present; exactly one of P0, P1, P2, P3 |
| Severity | Present |
| Test Steps | Numbered, complete or explicitly Blocked |
| Primary classification | Present; exactly one of Requirement-derived / Needs clarification / Recommendation |
| Assertion tags | Present; must include the primary classification |

**FAIL** if only legacy `Assertion type` is present without **Primary classification**.
**FAIL** rate-limit tests without N/T contract or weak "many requests" without expected throttle response.
**FAIL** SQLi/command payloads on surfaces where attack class is implausible.
**FAIL** if core STC count >8 without documented multi-surface justification.
**FAIL** if Tier-2 conditional tests (session swap, ID enumeration, CSP/framing, ZIP bomb, presigned URL replay) appear as executable STCs without explicit architecture/policy trigger in requirement/analysis.
**WARN** if core STC count <5 when feature has clear user-scoped resources or file upload surface.

### Suite structure
- **Security Test Cases** — Core tier only (target 7–8)
- **Conditional Security Tests (deferred)** — not counted in STC total
- Deferred tests must not be duplicated as full STCs without trigger

### Generation Summary integrity (critical)
Recompute counts from the test-case body — do not trust stated counts without verification.

**Mutually exclusive partitions** (each STC in exactly one row per dimension; sums must equal Total):
- Threat type (or category if used)
- Priority
- Primary classification

**Partition rule:** Every STC ID must appear **exactly once** in each mutually exclusive summary dimension.

For each mutually exclusive summary row:
1. Extract listed STC IDs
2. Count **unique** IDs only — duplicate IDs in one row **FAIL**
3. Verify Count equals unique ID count
4. Verify sum of row counts equals **Total security test cases**

**Assertion tags (non-mutually-exclusive):**
- Separate section clearly labeled non-mutually-exclusive
- Tag counts may exceed Total
- **FAIL** if tag counts presented as mutually exclusive or labeled "Count (primary)" without a primary classification table

**FAIL** if output contains draft counts, corrections, or Count ≠ unique ID list length.

Format your response in markdown:

## Security Test Validation Results
One subsection per STC (### STC-xxx) with field pass/fail.

## Generation Summary Audit
- Core STC count vs 7–8 target
- Tier-2 tests incorrectly promoted to STC
- Recomputed threat-type, priority, and primary classification counts
- Recomputed assertion tag counts (non-mutually-exclusive)
- Partition checks and duplicate ID detection
- Arithmetic verification table

## Summary
- Total STCs validated
- Per-field pass/fail counts
- Summary integrity failures (with evidence)
- Overall verdict: Valid / Needs fixes / Invalid

If summary integrity fails, overall verdict must be **Needs fixes** or **Invalid**.
